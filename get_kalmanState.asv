function [outputArg1,outputArg2] = get_kalmanState(inputArg1,inputArg2)
%GET_KALMANSTATE This function is tailored only to the ncv 3D model
% [x, y, z, x', y', z'] => kalman state
    x = zeros(6, 1);
    x(1) = xm;
    x(2) = xp_init;
    x(3) = ym;
    x(4) = yp_init;
    x(5) = zm;
    x(6) = zp_init;
end

